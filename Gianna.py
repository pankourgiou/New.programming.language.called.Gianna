import re

class GiannaInterpreter:
    def __init__(self):
        self.μεταβλητές = {}

    def εκτέλεσε(self, γραμμή):
        γραμμή = γραμμή.strip()
        if not γραμμή:
            return

        # ✨ Εντολή βοήθειας
        if γραμμή in ["βοήθεια", "help", "?"]:
            print("🌸 Καλώς ήρθες στη Γιάννα! 🌸")
            print("Διαθέσιμες εντολές:")
            print("  📌 γράψε \"κείμενο\"      -> εμφανίζει κείμενο στην οθόνη")
            print("  📌 γράψε όνομα           -> εμφανίζει τιμή μεταβλητής")
            print("  📌 όρισε όνομα = τιμή    -> δημιουργεί ή αλλάζει μεταβλητή")
            print("  📌 βοήθεια               -> εμφανίζει αυτό το μήνυμα")
            print("  📌 έξοδος                -> κλείνει τη Γιάννα")
            return

        # 🚪 Έξοδος
        if γραμμή in ["έξοδος", "quit", "exit"]:
            print("👋 Αντίο από τη Γιάννα! Να έχεις μια χαριτωμένη μέρα 🌸")
            raise SystemExit

        # 📝 Εντολή "γράψε"
        if γραμμή.startswith("γράψε"):
            περιεχόμενο = re.findall(r'"(.*?)"', γραμμή)
            if περιεχόμενο:
                print(περιεχόμενο[0])
            else:
                όνομα = γραμμή.replace("γράψε", "").strip()
                if όνομα in self.μεταβλητές:
                    print(self.μεταβλητές[όνομα])
                else:
                    print("🤔 Δεν ξέρω τη μεταβλητή:", όνομα)

        # 🎁 Ορισμός μεταβλητής
        elif γραμμή.startswith("όρισε"):
            try:
                _, υπόλοιπο = γραμμή.split("όρισε", 1)
                όνομα, τιμή = υπόλοιπο.split("=")
                όνομα, τιμή = όνομα.strip(), τιμή.strip()
                if τιμή.isdigit():
                    self.μεταβλητές[όνομα] = int(τιμή)
                else:
                    self.μεταβλητές[όνομα] = τιμή.strip('"')
                print(f"🎀 Η μεταβλητή '{όνομα}' ορίστηκε σε {self.μεταβλητές[όνομα]}")
            except:
                print("😵 Ουπς! Κάτι πήγε στραβά στον ορισμό.")

        else:
            print("🙃 Δεν καταλαβαίνω αυτή την εντολή. Πληκτρολόγησε 'βοήθεια' για οδηγίες.")

def repl():
    print("🌸 Καλώς ήρθες στη Γιάννα! 🌸")
    print("Πληκτρολόγησε 'βοήθεια' για οδηγίες ή 'έξοδος' για να φύγεις 💖")
    interpreter = GiannaInterpreter()

    while True:
        try:
            γραμμή = input("💎 Γιάννα> ")
            interpreter.εκτέλεσε(γραμμή)
        except KeyboardInterrupt:
            print("\n🙋 Έκανες Ctrl+C — Αντίο από τη Γιάννα 💕")
            break
        except SystemExit:
            break
        except Exception as e:
            print("😿 Σφάλμα:", e)

if __name__ == "__main__":
    repl()
